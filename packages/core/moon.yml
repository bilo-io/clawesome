# packages/core/moon.yml
$schema: 'https://moonrepo.dev/schemas/project.json'

project:
  name: 'core'

tags:
- 'core'
- 'library'
- 'package'

tasks:
  build:
    command: 'tsc -b'
    inputs:
      - 'src/**/*'
