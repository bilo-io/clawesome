import { DocPlaceholder } from "@clawesome/ui";

export default function SlackPage() {
  return (
    <DocPlaceholder 
      title="Slack Connector" 
      description="Directly pipe Clawesome events and agent interactions into your Slack channels." 
    />
  );
}
