import { DocPlaceholder } from "@clawesome/ui";

export default function SecurityPage() {
  return (
    <DocPlaceholder 
      title="System Security" 
      description="Hardened, encrypted communications and fine-grained permissions for every agent." 
    />
  );
}
